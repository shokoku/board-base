CREATE TABLE user (
                       id VARCHAR(255) NOT NULL PRIMARY KEY,
                       pw VARCHAR(255) NOT NULL,
                       name VARCHAR(255) NOT NULL,
                       phone VARCHAR(50),
                       email VARCHAR(255)
);

